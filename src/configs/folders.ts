// ================ DO NOT CHANGE THE FOLLOWING ====================
export const PublicFolder = 'public';

export enum PublicFolders {
  audios = 'audios',
  documents = 'documents',
  images = 'images',
  tempChats = 'temp-chats',
  uploads = 'uploads',
  videos = 'videos',
}

export const PrivateFolder = 'private';

export enum PrivateFolders {
  db = 'db'
}